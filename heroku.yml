setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    gateway-service: 
      dockerfile: Dockerfile
      context: .
    auth-service: 
      dockerfile: Dockerfile
      context: .
    travel-service: 
      dockerfile: Dockerfile
      context: .

run:
  web:
    image: gateway-service
    command:
      - docker-compose up